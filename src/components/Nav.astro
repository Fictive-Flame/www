<nav class="bg-gray-800 shadow-md p-4 font-humanist font-bold" x-data="{ open: false }">
  <div class="container mx-auto flex items-center justify-between">
    <div class="text-1xl text-gray-200 uppercase">
        <a href="/">Fictive Flame</a>
    </div>
    <div class="hidden md:flex space-x-6 font-light uppercase">
      <a href="/work" class="text-gray-200 hover:text-gray-400">Work</a>
      <a href="/about" class="text-gray-200 hover:text-gray-400">About</a>
      <a href="/contact" class="text-gray-200 hover:text-gray-400">Contact</a>
    </div>
    <div class="md:hidden">
      <button @click="open = true" class="text-gray-200 hover:text-gray-400 focus:outline-none">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
            d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </button>
    </div>
  </div>
  <div
    x-show="open"
    @click="open = false"
    class="fixed inset-0 bg-black bg-opacity-50 z-40"
    x-transition.opacity
  ></div>
  <div
    x-show="open"
    class="fixed top-0 right-0 w-64 h-full bg-gray-800 shadow-lg z-50 transform transition-transform duration-300 ease-in-out"
    :class="open ? 'translate-x-0' : 'translate-x-full'"
  >
    <div class="p-4 flex justify-between items-center border-b">
      <button @click="open = false" class="text-gray-200 hover:text-gray-400">✕</button>
    </div>
    <div class="flex flex-col p-4 space-y-4 font-light uppercase">
      <a href="/work" class="text-gray-200 hover:text-gray-400">Work</a>
      <a href="/about" class="text-gray-200 hover:text-gray-400">About</a>
      <a href="/contact" class="text-gray-200 hover:text-gray-400">Contact</a>
    </div>
  </div>
</nav>
